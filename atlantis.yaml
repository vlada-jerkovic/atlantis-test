version: 3
projects:
  - name: stage
    dir: envs/stage
    workspace: stage
    autoplan:
      when_modified: ["**/*.tf*"]
      enabled: true


  - name: prod
    dir: envs/prod
    workspace: prod
    autoplan:
      when_modified: ["**/*.tf*"]
      enabled: true